const express = require('express');
const router = express.Router();

module.exports = router;

// router.post('/addpost', userController.register);

// // Login user
// router.post('/login', userController.login);

// router.post('./logout', userController.logout)